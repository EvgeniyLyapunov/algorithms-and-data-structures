import { Container, Row, Col } from "react-bootstrap";

import './appHeader.scss';

const AppHeader = () => {

  return(
    <div className="app-header">
      <h1 className="app-header__title">Algorithms and data structures</h1>
    </div>
  )
}

export default AppHeader;